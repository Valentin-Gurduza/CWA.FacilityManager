@page "/profile"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]
@inject NavigationManager NavigationManager

@* This page now redirects to Account/Manage *@

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/Account/Manage", replace: true);
    }
}